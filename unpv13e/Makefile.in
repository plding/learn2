include ./Make.defines

DIRS = lib \
	   intro

all:
	@for dir in $(DIRS); do $(MAKE) -C $${dir}; done

clean:
	@for dir in $(DIRS); do $(MAKE) -C $${dir} clean; done

distclean: clean
	rm -rf $(CLEANFILES) config.cache config.log config.status config.h Make.defines Makefile
